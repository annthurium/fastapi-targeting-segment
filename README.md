## Example FastAPI app - Targeting Segments

This demo is for a tutorial on using LaunchDarkly Targeting Segments in a Python FastAPI Application.

## Setup

Set up your virtual environment:

`python -m venv venv`

Install dependencies:
`pip install -r requirements.txt`

Run the server by typing the following command in your terminal:

`fastapi dev main.py`

If you open http://127.0.0.1:8000/ in a browser, you should see a Hello World page.
